# Hierarchical world-model results

`world_model/`, `reaction_events/`, and `ppo_idm_response/` are the only
active result inputs. The response directory contains exactly one checkpoint,
its IDM rule prior, and its held-out paired audit.
