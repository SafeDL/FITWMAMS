# Hierarchical world-model results

`world_model/`, `reaction_events/`, and `nominal_response/` are the only
active result inputs. `archive/` contains historical or rejected experiments
and is excluded from active scripts and configuration.
